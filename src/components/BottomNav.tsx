export const BottomNav = () => {
  return (
    <section className="fixed inset-x-0 bottom-0 z-10 flex items-center justify-between bg-white p-4 shadow">
      <p>Go Back</p>
      <button className="rounded-md bg-blue-marine p-2 text-white">
        Next Step
      </button>
    </section>
  )
}
